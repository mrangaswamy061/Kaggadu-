{
  "name": "kaggadu-adventures",
  "version": "1.0.0",
  "description": "Root orchestrator for Kaggadu Adventures trekking full-stack application",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "server": "npm run start --prefix server",
    "build": "npm run build --prefix frontend",
    "start": "npm run build --prefix frontend"
  },
  "dependencies": {
    "monodb": "^1.5.1"
  }
}
